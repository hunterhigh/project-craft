interface:
  display_name: "Goal Discipline"
  short_description: "Keep work on the actual task without completeness-driven expansion"
  default_prompt: "Use $goal-discipline silently to keep this work on the actual task and stop when the requested result is complete."

policy:
  allow_implicit_invocation: true
